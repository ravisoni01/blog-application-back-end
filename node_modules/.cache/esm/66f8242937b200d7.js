let multer,GridFsStorage;_37b‍.x([["default",()=>_37b‍.o]]);_37b‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_37b‍.w("multer-gridfs-storage",[["GridFsStorage",["GridFsStorage"],function(v){GridFsStorage=v}]]);


const storage = new GridFsStorage({
    url: 'mongodb+srv://ravi:1234567890@cluster0.uend8.mongodb.net/blog?retryWrites=true&w=majority',
    options: { useNewUrlParser: true, useUnifiedTopology: true },
    file: (req, file) => {
        const match = ["image/png", "image/jpg"]
        if (match.indexOf(file.mimeType) === -1) {
            return `${Date.now()}-blog-${file.originalname}`
        }

        return {
            bucketName: "photos",
            filename: `${Date.now()}-blog-${file.originalname}`
        }
    }
})

_37b‍.d(multer({ storage }));
